import Home from './view';
import { connect } from "react-redux";

const mapStateToPropsHome = () => ({});
const mapDispatchToPropsHome = () => ({});

export default connect(mapStateToPropsHome, mapDispatchToPropsHome)(Home);
